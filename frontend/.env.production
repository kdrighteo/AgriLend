# This is for local production builds - when deployed to Vercel
# you'll need to set this as an environment variable in the Vercel dashboard
REACT_APP_API_URL=https://your-backend-api.vercel.app/api

# Fix for OpenSSL issues with newer Node.js versions
NODE_OPTIONS=--openssl-legacy-provider
